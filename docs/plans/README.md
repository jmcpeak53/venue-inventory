# Implementation plans

Keep focused implementation plans in this directory. Each plan should give a
coding agent enough context to complete one small, reviewable piece of work.

When a plan is finalized, include the suggested Anthropic and OpenAI
implementation models and the three most likely runtime failure points.
